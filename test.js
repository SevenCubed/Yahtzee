const result = [1, 1, 1, 0, 1, 1];
function straight(accumulator, currentValue) {
    if(currentValue != 0)
    {
        accumulator++
    }
}


console.log(result.reduce(straight, 0);
console.log('?')